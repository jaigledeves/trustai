{
  "name": "trustai",
  "version": "0.1.0",
  "private": true,
  "description": "TrustAI — intelligent certification of digital assets (AI + blockchain)",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}
