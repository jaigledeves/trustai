# Specs

Source-of-truth specs live here as `{domain}/spec.md`. Populated by `sdd-archive`
when a change's delta specs are merged into the main spec. Empty at SDD init —
no changes have been archived yet.
